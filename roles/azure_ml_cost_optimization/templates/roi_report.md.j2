# ML Platform Cost Optimization - ROI Report

**Report Date:** {{ report_date }}
**Resource Group:** {{ azure_resource_group }}

## Executive Summary

| Metric | Value |
|--------|-------|
| **Baseline Monthly Cost** | ${{ baseline_cost | int | string | regex_replace('(\\d)(?=(\\d{3})+$)', '\\1,') }} |
| **Current Month-to-Date Cost** | ${{ current_cost | float | round(2) | string | regex_replace('(\\d)(?=(\\d{3})+\\.)', '\\1,') }} |
| **Monthly Savings** | ${{ monthly_savings | float | round(2) | string | regex_replace('(\\d)(?=(\\d{3})+\\.)', '\\1,') }} ({{ savings_percent }}%) |
| **Projected Annual Savings** | ${{ annual_savings | float | round(2) | string | regex_replace('(\\d)(?=(\\d{3})+\\.)', '\\1,') }} |

---

## Cost Breakdown by Resource Type

{% if cost_breakdown | length > 0 %}
| Resource Type | Current Month Cost |
|---------------|-------------------|
{% for resource_type, cost in cost_breakdown.items() %}
| {{ resource_type }} | ${{ cost | float | round(2) }} |
{% endfor %}
{% else %}
*No cost breakdown available (Cost Management API query failed or returned no data)*
{% endif %}

---

## Automation Actions This Month

| Action Type | Count |
|-------------|-------|
| Compute Cluster Shutdowns | {{ shutdown_events }} |
| Clusters Right-Sized | {{ rightsize_events }} |
| Azure OpenAI PTU Optimizations | {{ ptu_events }} |

{% if shutdown_events | int > 0 %}
### Shutdown Events Details
Compute clusters were automatically shut down during non-business hours to eliminate idle costs. Estimated savings from shutdown events: ~{{ (shutdown_events | int * 14 * 30 * 1.5) | round(0) | int }} hours of compute time saved.
{% endif %}

{% if rightsize_events | int > 0 %}
### Right-Sizing Details
Underutilized clusters (CPU < 30% for 7 days) were automatically downsized to smaller VM sizes. This optimization balances cost with performance needs.
{% endif %}

{% if ptu_events | int > 0 %}
### PTU Optimization Details
Azure OpenAI Provisioned Throughput Units were scaled down due to low utilization (<50% for 7 days). This reduces fixed monthly costs while maintaining sufficient capacity.
{% endif %}

---

## Trend Analysis

*Trend data requires historical ROI reports. Run this report monthly to build a cost trend chart.*

---

## Recommendations

{% if savings_percent | float < 20 %}
- **Low savings detected ({{ savings_percent }}%).** Review baseline cost accuracy and ensure automation schedules are active.
- Consider expanding automation scope to additional workspaces or resource groups.
{% elif savings_percent | float >= 20 and savings_percent | float < 40 %}
- **Good progress ({{ savings_percent }}%).** Continue current automation patterns.
- Review right-sizing thresholds to identify additional optimization opportunities.
{% else %}
- **Excellent savings ({{ savings_percent }}%).** Current automation is highly effective.
- Monitor for potential over-optimization (e.g., clusters being downsized too aggressively).
{% endif %}

---

**Report generated by:** `cloud.azure_ops.azure_ml_cost_optimization` role
**Automation powered by:** Ansible Automation Platform
